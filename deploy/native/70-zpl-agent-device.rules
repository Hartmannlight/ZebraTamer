# Keep Zebra printer-class character devices private to root and the lp group.
# The zpl-agent service joins lp through SupplementaryGroups.
SUBSYSTEM=="usbmisc", KERNEL=="lp[0-9]*", ATTRS{idVendor}=="0a5f", GROUP="lp", MODE="0660", TAG+="systemd"
